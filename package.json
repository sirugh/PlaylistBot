{
  "name": "playlistbot",
  "engines": {
    "node": ">=18.0.0"
  },
  "type": "module",
  "version": "0.0.1",
  "description": "Reddit bot that generates playlists out of the top responses of a thread.",
  "main": "src/index.mjs",
  "scripts": {
    "lint": "eslint --fix src/**/*.mjs",
    "start": "node src/index.mjs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Stephen Rugh",
  "license": "ISC",
  "dependencies": {
    "express": "^5.2.1",
    "snoowrap": "^1.23.0",
    "spotify-web-api-node": "^5.0.2"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0",
    "globals": "^15.0.0"
  }
}
