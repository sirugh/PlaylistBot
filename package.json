{
  "name": "playlistbot",
  "engines": {
    "node": ">=18.0.0"
  },
  "type": "module",
  "version": "2.0.0",
  "description": "Generates Spotify playlists from highly-voted song recommendations on Reddit, using Claude AI for extraction.",
  "main": "src/index.mjs",
  "scripts": {
    "lint": "eslint --fix src/**/*.mjs",
    "start": "node src/index.mjs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Stephen Rugh",
  "license": "ISC",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.102.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0",
    "globals": "^15.0.0"
  }
}
